% Demo deck for the Bitcoin Austria Beamer style — 16:9 (the standard output).
% Brand-generic content (about Bitcoin Austria) — also the CI self-test.
% Build from this directory with:  latexmk -xelatex demo.tex   (-> demo.pdf)
%
% The 4:3 variant is demo-43.tex; both pull the same slides from demo-common.tex,
% so you can compare how the theme arranges identical content in either format.
\documentclass[aspectratio=169,t,9pt]{beamer}
\input{demo-common}
